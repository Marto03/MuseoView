public enum MuseumStatusEnum
{
    Active = 1,        // Активен
    UnderRenovation = 2,  // В ремонт
    Closed = 3,        // Затворен
    Relocation = 4,    // Престой (в преместване или реставрация)
    TemporarilyClosed = 5, // Временно затворен
    AwaitingOpening = 6    // Очаква откритие
}